import pygame

pygame.init() # do some magic

screen = pygame.display.set_mode((300,200)) # create screen object

while True:
    for event in pygame.event.get(): # check if some events happened and loop through them
        pass